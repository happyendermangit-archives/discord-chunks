{
    "exports": {
        "default": {
            "setProgress": "setProgress(e,t){if(!(0,r.isDesktop)())throw Error(\"cannot set progress from non-native\");if(null!=this.taskID&&this.taskID!==e){a.warn(`${this.taskID} is already running. cannot set ${e}`);return}this.taskID=e,o.default.window.setProgressBar(t)}",
            "clearProgress": "clearProgress(e){if(!(0,r.isDesktop)())throw Error(\"cannot clear progress from non-native\");if(null==this.taskID){a.warn(\"cannot clear progress. No current progress running.\");return}if(this.taskID!==e){a.warn(`cannot clear progress of ${e} when ${this.taskID} is running`);return}this.taskID=null,o.default.window.setProgressBar(-1)}"
        }
    }
}